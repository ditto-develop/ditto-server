---
paths:
  - "domain/**/member/**"
  - "api/**/user/**"
  - "api/**/admin/member/**"
---
회원(member) 코드를 수정하기 전에 `docs/domains/member.md`를 먼저 읽어라 — 회원 상태·온보딩 필수 정보·역할 규칙이 거기 있다. (api 회원 엔드포인트는 user/ 패키지)
