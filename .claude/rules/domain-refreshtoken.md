---
paths:
  - "domain/**/refreshtoken/**"
  - "api/**/auth/**"
---
리프레시 토큰(refreshtoken) 코드를 수정하기 전에 `docs/domains/refreshtoken.md`를 먼저 읽어라 — 토큰 유니크·만료 판정·회원 단위 삭제 규칙이 거기 있다. (발급/갱신 흐름은 `docs/domains/auth.md` 참조)
